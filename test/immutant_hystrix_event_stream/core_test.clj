(ns immutant-hystrix-event-stream.core-test
  (:require [clojure.test :refer :all]
            [immutant-hystrix-event-stream.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
